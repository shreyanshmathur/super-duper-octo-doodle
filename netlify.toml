[build]
  base    = "solar-app"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA — all routes served by index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
